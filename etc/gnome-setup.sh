#!/usr/bin/env bash

# Hotkeys

gsettings set org.gnome.desktop.wm.keybindings switch-to-workspace-up "['']"  # Free the bind for mc in emacs
gsettings set org.gnome.desktop.wm.keybindings switch-to-workspace-down "['']"  # Free the bind for mc in emacs
